<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-sdk
        android:minSdkVersion="7"
        android:targetSdkVersion="18" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="com.androidexample.gcm.permission.C2D_MESSAGE" />

    <!-- This app has permission to register and receive data message. -->
    <uses-permission android:name="com.google.android.c2dm.permission.RECEIVE" />

    <!-- Network State Permissions to detect Internet status -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Permission to vibrate -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:name=".Experimental.AppData">

        <meta-data android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <activity
            android:name=".Activities.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activities.LoginActivity"
            android:label="Login Activity" />
        <activity
            android:name=".Activities.RegistrationActivity"
            android:windowSoftInputMode="adjustResize"
            android:label="Registration Activity" />
        <activity
            android:alwaysRetainTaskState="true"
            android:name=".Activities.HomeActivity"
            android:label="home_activity" />
        <activity
            android:name=".Activities.SearchResultsActivity"
            android:label="Search Results" />
        <activity
            android:name=".Activities.CarShareDetailsActivity"
            android:label="Car Share Details" />
        <activity
            android:name=".Activities.PostNewCarShareActivity"
            android:label="New Car Share" />

        <activity
            android:name=".Activities.ManageAsDriverActivity"
            android:label="Manage Car Share" />

        <activity
            android:name=".Activities.ManageAsPassengerActivity"
            android:label="Manage Car Share" />

        <activity
            android:name=".Activities.ContactDriverActivity"
            android:label="Manage Car Share" />

        <activity
            android:name=".Activities.CarShareRequestsActivity"
            android:label="Manage Car Share" />

        <activity
            android:name=".Activities.CarShareRequestDetailsActivity"
            android:label="Manage Car Share" />

        <activity
            android:name=".Activities.TravelBuddyListActivity"
            android:label="Manage Car Share" />

        <receiver
            android:name=".GCM.GcmBroadcastReceiver"
            android:permission="com.google.android.c2dm.permission.SEND" >
            <intent-filter>
                <action android:name="com.google.android.c2dm.intent.RECEIVE" />
                <category android:name="com.example.gcm" />
            </intent-filter>
        </receiver>
        <service android:name=".GCM.GcmIntentService" />

    </application>

</manifest>
